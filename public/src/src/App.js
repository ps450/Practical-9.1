import React from 'react';
import './App.css';

function App() {
  return (
    <div className="App">
      <h1>🚀 React App Dockerized with Multi-Stage Build</h1>
      <p>Your React app is running inside a Docker container served by Nginx!</p>
    </div>
  );
}

export default App;
